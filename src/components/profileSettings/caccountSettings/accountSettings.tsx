import { FC } from "react"
import styles from "./accountSettings.module.scss"

export const AccountSettings: FC = () => {
  return (
    <>
      <div className={styles.container}>
        account settings blocks
      </div>
    </>
  )
}